# trusted_procs.txt - Слой 0.5 (ADR-0058 follow-up): доверенные системные демоны.
# Порты, биндуемые этими процессами, ЭФЕМЕРНЫ и легитимны (рантайм контейнеров и
# т.п.) - не регистрируются в привратнике и НЕ алертятся. Exemption по РЕАЛЬНОМУ
# бинарю владельца сокета (/proc/$pid/exe), а не по номеру порта (иначе кошки-мышки
# на эфемерных портах: 36401 -> 46199 -> 39727 -> ...).
# Формат: полный путь к бинарю на строку. Поддерживается glob (напр. /usr/bin/docker*).
# Строки с # - комментарий.

# Container runtime - эфемерные CRI/streaming/metrics порты:
/usr/bin/containerd

# (опционально) Docker daemon - эфемерные порты того же семейства:
# /usr/bin/dockerd
